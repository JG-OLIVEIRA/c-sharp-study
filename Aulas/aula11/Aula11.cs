using System;

class aula11{
    static void Main(){

        // Conversões implícitas
        int vInt = 10;
        float vFloat = vInt;

        Console.WriteLine(vShort);

        // Conversões explícitas
        short vShort=(short)vInt;

        Console.WriteLine(vShort);
    }
}